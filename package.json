{
  "name": "smart-todo-action",
  "version": "0.1.0",
  "description": "GitHub Action inteligente para transformar TODOs em issues e tarefas rastreáveis.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "prepare": "yarn ncc build src/ActionMain.ts -o dist",
    "build:dist": "if [ -d src/core/__tests__ ]; then mv src/core/__tests__ src/core/__tests__._; fi && ncc build src/ActionMain.ts -o dist && if [ -d src/core/__tests__._ ]; then mv src/core/__tests__._ src/core/__tests__; fi"
  },
  "keywords": [
    "github-action",
    "todo",
    "issues",
    "task-tracking",
    "llm",
    "automation"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@octokit/rest": "^21.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.17",
    "@vercel/ncc": "^0.38.3",
    "@vitest/coverage-v8": "^3.1.1",
    "typescript": "^5.3.3",
    "vitest": "^3.1.1"
  }
}
